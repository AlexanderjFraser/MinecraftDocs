/book
mermaid.min.js
# mermaid-init.js is committed: it is the figure theme (pass 7, session A), not
# the file `mdbook-mermaid install` writes. Do not re-run that command.
.DS_Store
# The decompile never enters the public repo (EULA / mappings licence)
/reference/
*.zip
/.wrangler/

# node deps for tools/check_mermaid.js (npm install in tools/)
/tools/node_modules/
__pycache__/

# rendered figures (tools/render_figures.js): screenshots and index.json, rebuilt on demand
/render/

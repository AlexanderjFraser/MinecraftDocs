<figure class="map">
{{#include ../generated/parts-dependency.svg}}
</figure>

*The thirteen parts top to bottom in watch order; an arc on the right runs from a part to one that assumes it, always downward, and the two dashed arcs on the left are the only places a part assumes a later one, each labelled with the pages it needs.*
